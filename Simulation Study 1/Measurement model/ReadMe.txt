Measurement model folder.

Same data generation procedure as the main simulation. It was done to evaluate the recovery of the measurement model parameters given that those were not evaluated in the main simulation. 